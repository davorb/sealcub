/*global define */

define(['video', 'bubbles', 'overview'], function(Video, Bubbles, Overview) {
  'use strict';
  Video.initialize();
  //Overview.initialize();
  Bubbles.initialize();
 
  
  return "";
});

